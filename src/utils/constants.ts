const MINUTE_IN_MS = 60 * 1000;

/** Finals are 4 minutes long. */
export const FINALS_DEFAULT_TIME_MS = 4 * MINUTE_IN_MS;

/** Semi-finals are 3 minutes long. */
export const SEMI_FINALS_DEFAULT_TIME_MS = 3 * MINUTE_IN_MS;
